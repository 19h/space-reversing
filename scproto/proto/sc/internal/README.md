# sc.internal protocol buffers

This is the repository for `internal` Star Citizen protocol buffers.

Protocols defined in this repository are considered `internal` and will never be shared with public clients.

!> Files in this package can depend on `sc.external` protocols.

## common

Primary proto package for all common protocols that are exclusively for internal use.

## services

Services API and types for all internal-facing Star Citizen services.
