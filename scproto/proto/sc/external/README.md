# sc.external protocol buffers

This is the repository for `external` Star Citizen protocol buffers.

Protocols defined in this repository are considered public and will be shared within the game clients.

!> No file in this package should depend on any `sc.internal` buffers.

## common

Primary proto package for all common protocols that are also publicly shared.

## services

Services API and types for all client-facing Star Citizen services.
